const menuButtonStyles = {
    position: 'absolute',
    top: '20px',
    right: '20px',
};
  
const MenuButton = () => {
    return (
        <button style={menuButtonStyles}>☰</button>
    )
}

export default MenuButton;